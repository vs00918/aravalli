# Mind of Aravalli Environment Configuration
# Local SQLite database path (default for local development)
DATABASE_URL="file:./dev.db"

# Future PostgreSQL / Production Database Connection
# DATABASE_URL="postgresql://user:password@localhost:5432/aravalli?schema=public"
